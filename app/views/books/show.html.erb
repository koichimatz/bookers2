<%= render 'users/user', user:@users, new_book:@book %>
<h1> Books</h1>

<div class="alert alert-primary" role="alert">
    <%= notice %>
  </div>
<% end %>
<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>
<p>
  <strong>Opinion:</strong>
  <%= @book.body %>
</p>

<%= link_to"Edit", edit_book_path(@book.id) %>
<%= link_to"Back", books_path %>